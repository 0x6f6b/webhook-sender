# webhook-sender

A tool for mass testing discord webhooks

Example:
Change the projectWebhooks variable
```
const projectWebhooks = {
  "server name": {
    id: "webhook id",
    token: "webhook token",
  },
  "server 2 name": {
    id: "webhook id",
    token: "webhook token",
  },
}
```
